package com.astamuse.asta4d.web.sitecategory.i18n;

/**
 * Currently, this interface is treated as an annotation only, but in future, we will add a default method for it(After we migrated to Java
 * 8).
 * 
 * @author e-ryu
 * 
 */
public interface ClassLocationBindedMessageResource {

}
